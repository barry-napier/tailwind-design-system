{
  "name": "tailwind-design-system",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./src/styles.css -o ./src/output.css --watch",
    "serve": "live-server --open=./src/index.html",
    "dev": "npm-run-all --parallel build:css serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.3",
    "prettier": "^3.5.3",
    "prettier-plugin-tailwindcss": "^0.6.11",
    "tailwindcss": "^4.1.3"
  },
  "dependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5"
  }
}
